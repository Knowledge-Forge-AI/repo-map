"""Future extractor package namespace.

PKG1 creates this package as an import-safe skeleton only. Extractor
implementation modules remain at the ``repomap_kg`` package root until later
package migration phases move them behind compatibility facades.
"""
