# Fake Codex VC Instructions

This fixture is public-safe fake policy content for MCP-OPS6 tests.

Use server-memory as a compact card catalog for pointers to policy docs.
Use RepoMap generated graph evidence to find files and relationships, not as a
replacement for user-authored policy.

Do not place machine-specific secrets, runtime database passwords, or raw
server-memory JSONL content in public policy docs.
