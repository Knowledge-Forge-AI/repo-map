# Static extraction fixture only. Do not execute.

typeset -A aliases
typeset -A colors=(red '#ff0000' blue '#0000ff')
local -A routes=([api]=/api [admin]=/admin)
typeset -A secrets=([token]="FAKE_ZSH_ARRAY_TOKEN")
