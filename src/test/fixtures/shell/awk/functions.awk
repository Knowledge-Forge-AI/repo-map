# Static extraction fixture only. Do not execute.

function normalize(value,    trimmed, suffix) {
  trimmed = value
  suffix = "-fixture"
  return trimmed suffix
}

function passthrough(value) {
  return value
}
